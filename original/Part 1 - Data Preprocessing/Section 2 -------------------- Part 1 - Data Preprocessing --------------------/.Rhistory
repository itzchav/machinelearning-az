#Codificar datos Categóricos
#Codificar datos Categóricos
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
dataset$Salary = ifelse(is.na(dataset$Salary),
ave(dataset$Salary, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Salary)
#Codificar datos Categóricos
#Codificar datos Categóricos
#Codificar datos Categóricos
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
setwd("D:/CursoML/original/Part 1 - Data Preprocessing/Section 2 -------------------- Part 1 - Data Preprocessing --------------------")
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
setwd("D:/CursoML/original/Part 1 - Data Preprocessing/Section 2 -------------------- Part 1 - Data Preprocessing --------------------")
setwd("D:/CursoML/original/Part 1 - Data Preprocessing/Section 2 -------------------- Part 1 - Data Preprocessing --------------------")
dataset$Salary = ifelse(is.na(dataset$Salary),
ave(dataset$Salary, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Salary)
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
dataset$Salary = ifelse(is.na(dataset$Salary),
ave(dataset$Salary, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Salary)
source('D:/CursoML/original/Part 1 - Data Preprocessing/Section 2 -------------------- Part 1 - Data Preprocessing --------------------/Seccion_2.R')
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
dataset$Salary = ifelse(is.na(dataset$Salary),
ave(dataset$Salary, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Salary)
#Codificar datos Categóricos
View(dataset)
dataset$Country = factor(dataset$Country,
levels = c("France", "Spain", "Germany"),
lables = c (1,2,3))
dataset$Country = factor(dataset$Country,
levels = c("France", "Spain", "Germany"),
lables = c (1,2,3))
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
dataset$Salary = ifelse(is.na(dataset$Salary),
ave(dataset$Salary, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Salary)
#Codificar datos Categóricos
dataset$Country = factor(dataset$Country,
levels = c("France", "Spain", "Germany"),
lables = c (1,2,3))
View(dataset)
View(dataset)
# Plantilla para preprocesar datos
# Seleccionar la lireria en Packages
# Importar el dataset
dataset = read.csv('Data.csv')
#Tratamiento de los valores NA
dataset$Age = ifelse(is.na(dataset$Age),
ave(dataset$Age, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Age)
dataset$Salary = ifelse(is.na(dataset$Salary),
ave(dataset$Salary, FUN =function(x) mean(x, na.rm = TRUE)),
dataset$Salary)
#Codificar datos Categóricos
dataset$Country = factor(dataset$Country,
levels = c("France", "Spain", "Germany"),
labels = c (1,2,3))
View(dataset)
dataset$Purchased = factor(dataset$Purchased,
levels = c("No", "Yes"),
labels = c (0,1))
View(dataset)
View(dataset)
View(dataset)
install.packages("caTools")
install.packages("bitops", repos = "https://cloud.r-project.org/")
install.packages("caTools", repos = "https://cloud.r-project.org/")
#Dividir los datos en conjunto de entrenamiento y conjunto de test
install.packages("bitops", repos = "https://cran.rstudio.com/")
chooseCRANmirror()
install.packages("caTools")
install.packages("bitops")
install.packages("caTools")
